# `uv-dynamic-versioning` Example

An example project using `uv-dynamic-versioning`.

```bash
pip install uv
# build this project
uvx --from build pyproject-build
# check built wheel & tar.gz files in dist/
```
